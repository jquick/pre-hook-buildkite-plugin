name: Pre Hook
description: Run commands as a pre-hook
author: https://github.com/jquick
configuration:
  properties:
    command:
      type: string
    commands:
      type: [ string, array ]
    script:
      type: string
  additionalProperties: false
